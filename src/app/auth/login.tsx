import { Text, View } from "react-native";

//write code to let user login via mobile number and otp

export default function Login() {
  return (
    <View
      style={{
        flex: 1,
        justifyContent: "center",
        alignItems: "center",
      }}
    >
      <Text>Edit app/auth/login.tsx to edit this screen.</Text>
      
    </View>
  );
}